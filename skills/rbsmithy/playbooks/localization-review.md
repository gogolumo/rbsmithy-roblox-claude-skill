# Localization Review Playbook

Check for hardcoded player-facing strings, missing keys, bad context, long-string UI breakage, and text used as IDs. Produce a key table and update CSV/Luau fallback files.
