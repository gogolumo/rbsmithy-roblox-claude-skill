# Generate Procedural 3D Model Prompt

> Generate a Roblox Studio procedural 3D model for `<asset>`. Use Claude Code + Rojo workflow. Create a typed Luau blueprint module and builder/spawn instructions. Use part-based low-poly geometry unless I explicitly provide real mesh asset IDs. Include scale in studs, folder paths, Command Bar spawn code, optimization notes, and Studio review steps.
