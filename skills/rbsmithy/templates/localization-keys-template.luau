--!strict

local LocalizationKeys = {
    MenuPlay = "menu.play",
    MenuSettings = "menu.settings",
    QuestStart = "quest.start",
    QuestComplete = "quest.complete",
}

table.freeze(LocalizationKeys)
return LocalizationKeys
