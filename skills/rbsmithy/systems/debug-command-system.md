# Debug Command System

Use dev-only commands for testing: teleport, give test item, spawn ship, start quest, reset local profile. Commands must check user ID/group/dev flag on the server and should be disabled or restricted in production.