# Invite System

Implement invites as server-created expiring records. Include sender, target, created time, expiry, context, and rate limits. Client only displays invite UI and sends accept/decline requests.