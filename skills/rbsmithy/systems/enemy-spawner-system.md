# Enemy Spawner System

Spawn enemies from server-owned zones. Include max alive count, respawn delay, despawn distance, team/party scaling, anti-farm limits, and cleanup. Avoid spawning huge groups on low-end devices.