# Model Budget System

Assign each model a budget tier from camera distance, reuse count, gameplay importance, and target devices. Output silhouette, hierarchy, triangle/part guidance, collision plan, LOD/fallback plan, and review checklist.