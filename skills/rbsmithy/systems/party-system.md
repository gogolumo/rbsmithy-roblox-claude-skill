# Party System

Support small co-op groups: leader, invite, accept, decline, leave, kick, disband, teleport together, shared objective state, reconnect handling, and party privacy. Server owns party membership.