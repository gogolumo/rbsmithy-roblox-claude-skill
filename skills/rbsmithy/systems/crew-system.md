# Crew System

For ship games, crew state should define ship ownership, roles, permissions, shared rewards, voyage state, and respawn behavior. Never let client claim captain/admin role without server validation.