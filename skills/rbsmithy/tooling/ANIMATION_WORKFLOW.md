# Animation Workflow

Prototype with placeholder IDs, publish animations in Studio, update `AnimationRegistry`, test rig compatibility, and document ownership/access. Never invent real animation IDs.
