# TestEZ Setup

Use TestEZ when the project wants BDD-style Roblox/Luau unit tests. Add it through Wally or an existing vendored package. Keep specs under `src/tests` or an existing test folder. Provide a runner and document how to run it in Studio or CLI workflow.

Do not add TestEZ if the project already standardized on Jest Roblox unless requested.
