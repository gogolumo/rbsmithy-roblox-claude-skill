# Localization CSV Workflow

1. Extract player-facing strings into stable keys.
2. Generate/update `localization-table.csv`.
3. Add context for translators.
4. Import/export through Creator Hub localization table workflow where applicable.
5. Test long strings and missing-key fallback.
